#!/usr/bin/bash

# to define a variable in bash write down the variable name (vairableName) followed by equals sign without any space.
variable1="This is the first variable without any space"
variable2="THis is another variable"

# then, use the variable using $ sign followed by variable name 
echo $variable1
echo -n $variable2
